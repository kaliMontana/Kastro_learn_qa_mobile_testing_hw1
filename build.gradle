plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

compileJava {
    options.encoding = 'UTF-8'
}

def junitVersion = "5.8.2"

dependencies {
    testImplementation(
            "org.junit.jupiter:junit-jupiter:$junitVersion"
    )
}

test {
    useJUnitPlatform()
}